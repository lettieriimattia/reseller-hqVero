// ARCHIVIO — Riquadri riassuntivi Profitto / Ricavi / Pezzi venduti / Margine / Acquistato
// Blocco JSX tolto da components/AnalyticsExplorer.tsx (righe 304-311 al commit "pre-cleanup analitiche").
// Il componente MetricTile e i calcoli (cur, prev, valueOf, series) restano in AnalyticsExplorer.tsx, non mostrati.
// Non compilato (.txt): per reintegrarlo, rincollalo nel punto segnato. Vedi REMOVED_SECTIONS.md.

      {/* Metriche: tocca per mostrarla nel grafico */}
      <div role="tablist" aria-label={t('ex.metric')} className="grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-5 gap-2">
        {METRICS.map(m => (
          <MetricTile key={m} m={m} active={metric === m} onClick={() => setMetric(m)}
            value={valueOf(cur, m)} prev={valueOf(prev, m)} spark={series.map(s => valueOf(s.agg, m))}
            label={t(`ex.m.${m}`)} fmt={fmt} t={t} sub={m === 'count' ? null : m === 'spent' ? null : `${int(cur.count)} ${cur.count === 1 ? t('ex.sale') : t('ex.sales')}`} />
        ))}
      </div>
